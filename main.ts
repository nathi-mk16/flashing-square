input.isGesture(Gesture.Shake)
led.unplot(1, 0)
input.buttonIsPressed(Button.A)
music.playMelody("E D G F B A C5 B ", 40)
for (let index = 0; index < 700; index++) {
    basic.showString("Miyelani mashele")
}
